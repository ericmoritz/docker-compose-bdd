version: '3'

services:
  web:
    build:
      context: ..
    command: "python -m http.server 8080"
    ports:
      - "8080:8080"
  bdd:
    build: .
    command: "./node_modules/.bin/cucumber-js"
    depends_on:
      - web
